# Overview

First iteration of Yashita Namdeo's personal portfolio website, built with Gatsby and hosted on GitHub Pages. Development runs via the Gatsby CLI (`npm start`), with production output from `npm run build`.
